name: jobs & steps
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build started
        run: echo "build started"
      - name: clone the code
        run: git clone https://github.com/azlan-43/github-actions.git
      - name: check the code
        run: ls -a
